async def switch_node_mode(node_id: str, mode: str) -> bool:
    print(f"Simulating mode switch for node {node_id} to {mode}")
    # In a real system, this would send a command to the node
    return True
